/**
 * HeroPlayers — legacy component.
 * Player visuals are now composed inside HeroSection.jsx.
 * This file is kept to avoid import errors if referenced elsewhere.
 */
export default function HeroPlayers() {
  return null;
}
